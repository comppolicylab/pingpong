# Example environment variables for the local PingPong scripts
# Copy this file to .env and replace the values with your actual configuration
# In this directory, run `cp .env.example .env`
# In the parent directory, run `cp pingpong/scripts/.env.example pingpong/scripts/.env`

# AIRTABLE_API_KEY is your personal access token for accessing the Airtable API
# Replace with your actual Airtable personal access token
AIRTABLE_API_KEY=key1234567890abcdef

# AIRTABLE_BASE_ID is the ID of your Airtable base
# Replace with your actual Airtable base ID
AIRTABLE_BASE_ID=appABCDEFGHIJKLMN

# AIRTABLE_TABLE_NAME_CLASSES is the name of the Airtable PingPong Classes table you are accessing.
# Replace with the actual table name
AIRTABLE_TABLE_NAME_CLASSES="Classes"

# AIRTABLE_TABLE_NAME_ASSISTANT_TEMPLATES is the name of the Airtable Assistant Templates table you are accessing.
# Replace with the actual table name
AIRTABLE_TABLE_NAME_ASSISTANT_TEMPLATES="Assistant Templates"

# AIRTABLE_TABLE_NAME_ASSISTANTS is the name of the Airtable PingPong Assistant table you are accessing.
# Replace with the actual table name
AIRTABLE_TABLE_NAME_ASSISTANTS="Assistants"

# AIRTABLE_TABLE_NAME_USER_RECORDS is the name of the Airtable User Class Roles Records table you are accessing.
# Replace with the actual table name
AIRTABLE_TABLE_NAME_USERCLASSROLES="User Records"

# BILLING_PROVIDERS should be a JSON string representing a dictionary
# It maps provider names to their IDs
# Example:
# BILLING_PROVIDERS={"PROVIDER_ONE": 101, "PROVIDER_TWO": 202, "NO_SELECTION": null}
BILLING_PROVIDERS={"EXAMPLE_PROVIDER": 123, "NO_SELECTION": null}

# PINGPONG_COOKIE is your PingPong authentication cookie
# Replace with your actual PingPong cookie
PINGPONG_COOKIE=your_pingpong_cookie_here

# PINGPONG_URL is the URL of your PingPong instance
# Replace with your actual PingPong URL
PINGPONG_URL="http://localhost:5173"
